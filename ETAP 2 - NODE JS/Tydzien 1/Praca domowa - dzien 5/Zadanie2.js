/*
2. Zawartość wszystkich plików w katalogu

Napisz program, który wyświetli zawartość wszystkich plików w folderze PROJEKTY

Skorzystaj z możliwości listowania plików w folderze. Po otrzytmaniu listy plików wyświetlaj je w pętli.

Następnie spórbuj wykorzystać STAT -> konkretny wpis ma metodę .isDirectory()  aby wchodzić rekursywnie do kolejnych katalogów również wszystko w środku.

 */

async function showFilesInDirectory(path){

    if(.....){
        await showFilesInDirectory(.....)
    }

}

showFilesInDirectory('.');